<h1>Welcome to your tasks list site!</h1>
<section>(this is the index action page)</section>

<section>
  <h2>Here are all the tasks:</h2>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= link_to task.name, task_path(task.id) %>
        <%# <%= link_to "(Delete)", task_path(task.id), method: :delete, data: { confirm: "Are YOU sure?" } %>
      </li>
    <% end %>
  </ul>
</section>

<section>
  <%# put an image of kaya here %>
</section>