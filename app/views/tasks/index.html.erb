<h1>To-do:</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
    TASK: <%= link_to task.name, task_path(task) %></li>
    <p>date assigned: <%=task.assignment_date%></p>
  <% end %>
</ul>

<%= link_to "New Task", new_task_path %>