<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <%= link_to task.name, task_path(task)  %> Description: <%= task.description %>  <%= task.completion_date %> 
    </li>
  <% end %>
</ul>