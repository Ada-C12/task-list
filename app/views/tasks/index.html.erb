<h2>Tasks</h2>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <%= link_to task.name, task_path(task) %>
      <%= task[:name] %> : <%= task[:description] %> : <%= task[:completed] %>
    </li>
    
  <% end %>
</ul>