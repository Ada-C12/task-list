<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.name, task_path(task) %>: <%= task.description %> <%= link_to "Edit", edit_task_path(task) %> 
    </li>
  <% end %>
</ul> 
