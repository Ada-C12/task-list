<section>
    <h2>To do list</h2>
    <ul>
        <% @tasks.each do |task| %>
            <li>
            <%= task.name %> -- <%= task.description %>  
            <%= link_to("Edit", edit_task_path(task.id)) %>
            <%= link_to("Delete", task_path(task.id), method: :delete, data: {confirm: "Delete this task!"}) %>
            </li>
        <%end%>
    </ul>
        <%= link_to("Add new task!", new_task_path) %>
</section>