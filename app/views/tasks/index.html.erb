<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.name, task_path(task) %>: <%= task.description %>  <%= link_to "Delete", task_path(task), method: :delete, data: {confirm: "Are you sure you want to delete this task?"}%>
    </li>
  <% end %>
</ul> 
