<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <%= link_to task.name, task_path(task) %><br> 
      <%= task.description %><br>
      Completed: <%= task.completion_date %><br>
    </li>
  <% end %>
</ul>
