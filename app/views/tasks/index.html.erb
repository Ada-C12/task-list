<h2>Clara's To Do List</h2>

<section class="task list">
 <ol>
  <% @tasks.each do |task| %>
   <li>
    <%= link_to task.name, task_path(task.id) %>
   </li>
  <% end %>
 </ol>
</section>

<section class="Add a task">
 <%= link_to "Add a new task!", "/tasks/new"%>
</section>