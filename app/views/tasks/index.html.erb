<p><% @tasks.each do |task|%>
<p><%= link_to task.id%><%= task.description%> completed <%= task.completed%></p>
<% end %>
<h2>list o' all tasks</h2>
<ul><% @tasks.each do |task|%>
<li><%= link_to task.id%><%= task.description%> completed <%= task.completed%> <%= link_to"(Delete)", task_path( task.id ), method: :delete, data: { confirm: "Are you sure you want to delete this task?"} %>
</li><%= link_to"(toggle_completed)", task_path( task.id ), method: :toggle_completed,
 <li>
<%# Mark a task complete

    Add a button to the list of tasks on the home page that, when clicked, will mark a task complete
    Update the database with the task's completed date %>
</li>
<% end %>

</ul>