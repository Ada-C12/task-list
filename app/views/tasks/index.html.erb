<h1>All Tasks</h1>

<section>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= link_to task.name, task_path(task.id) %>
      </li>
    <% end %>
  </ul>
</section>

<section>
  <p>
    <%= link_to "Add New Task", new_task_path %>
  </p>
</section>


