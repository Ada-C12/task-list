<h1>Tasks</h1>

<section>
  <h2>These things need to be done!</h2>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= "#{task[:title]}" + "  --  " +  "#{task[:date]}" %>
        <% task[:status] %>
      </li>
    <% end %>
  </ul>
</section>
