<h1>Tasks</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <%= task.name %>
      <%= task.description %>
      <%= task.completed %>
      <%= link_to "Edit", edit_task_path(task) %>
    </li>
  <% end %>
</ul>