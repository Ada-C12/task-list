<h1>My tasklist</h1>

<ol>
  <% @tasks.each do |task| %>
    <li>
      <%= task.name %> is the name of the task. Which is just when you <%= task.description %>. You must complete this by <%= task.completion_date %>.
    </li>
  <% end %>
</ol>