<h1>Tasks</h1>
<h1> Home Page: Tasks </h1>

<section>
<h2> Tasks </h2>
<ul>
 <% @tasks.each do |task| %>
  <li>
   <%= link_to task.name, task_path( task.id ) %> : <%= task.description %>
   </li>
 <% end %>
</ul>
</section>
