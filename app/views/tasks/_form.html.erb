<%= form_with model: @task, class: 'task-input' do |f| %>
<p>Please provide the following information to <%= action_name.downcase %> your task:</p>

  <%= f.label :name %>
  <%= f.text_field :name%>
  <%= f.label :description %>
  <%= f.text_area :description%>

  <%= f.submit action_name.capitalize, class: "task-button" %>
<% end %>