
<%= form_with model: @task, class: form_class do |f| %>
  <p><%= f.label :name %>
  <%= f.text_field :name %></p>
  <p><%= f.label :description %>
  <%= f.text_field :description %></p>

  <p><%= f.submit submit_button_label, class: form_class %></p>
<% end %>