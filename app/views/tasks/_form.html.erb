<p>Please provide the following information to <%= verb_name %> a task:</p>


<%= form_with model: @task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>
  
  <%= f.submit action_name, class: "submit-button" %>
<% end %>