<h1><%= @task.name %></h1>
<%= link_to "Edit this task", edit_task_path(@task.id) %>
<ul>
    <li>
      <%= @task.description %>
      <%= @task.completed %>
    </li>
</ul>
<%= link_to "Toggle Complete", toggle_complete_path(@task.id), method: :patch %>
