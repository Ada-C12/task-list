<h1> This is an individual task page! <h1>

<h2> Current task is: <%= @task.name %> </h2>

<ul>
  <li> Description: <%= @task.description %> </li>
  <li> Completion Date: <%= @task.completion_date %> </li>
  <li> Edit: <%= link_to "Edit Task", edit_task_path(@task.id) %> </li>
  <li> Delete: <%= link_to "Delete Task", task_path(@task.id), method: :delete, data: {Confirm: "Hold up! Are you sure you want to delete this item?"} %> </li>
  <li> <% if @task.completion_date == nil %>
          <%= link_to "Mark Complete", toggle_path(@task.id), method: :patch %>
        <% else %>
          <%= link_to "Mark Incomplete", toggle_path(@task.id), method: :patch %>
        <% end %>
  </li>
</ul>