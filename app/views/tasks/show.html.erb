<p> <%= @task.description %> and should be completed 
by <%=@task.completed %></p>
<p><%= link_to "Edit", edit_task_path(@task)%></p>
<p><%= link_to "Delete", task_path(@task), method: :delete, data: {confirm: "Are you sure?"}%>

