
<h2>Add New Task</h2>

<%= form_with model: @task, class: 'user_input' do |f| %>

<%= f.text_field :tasks %>
<%= f.text_field :description %>

<%= f.label :tasks %>
<%= f.text_field :tasks %>

<%= f.label :description %>
<%= f.text_field :description %>

<%= f.submit "Save Task", class: "book-button" %>

<% end %>